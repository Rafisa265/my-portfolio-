# my-portfolio-
A multi-stack dynamic website monorepo containing Node.js (Express), Python (Flask), and PHP backend examples with a shared static frontend. It includes configuration files, sample data storage, and ready-to-run setups for each backend, making it easy to learn, test, and deploy different web stacks in one project.
